import 'package:flutter/material.dart';

class DockerPage extends StatelessWidget {
  const DockerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Docker — coming soon'));
  }
}

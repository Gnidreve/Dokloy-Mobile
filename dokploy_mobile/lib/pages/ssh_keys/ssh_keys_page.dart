import 'package:flutter/material.dart';

class SshKeysPage extends StatelessWidget {
  const SshKeysPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('SSH Keys'));
  }
}

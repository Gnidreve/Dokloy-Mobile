import 'package:flutter/material.dart';

class GitPage extends StatelessWidget {
  const GitPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Git'));
  }
}
